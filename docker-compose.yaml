# version: '3.8'
# services:
#   ollama:
#     image: ollama/ollama:latest
#     ports:
#       - 7869:11434
#     volumes:
#       - .:/code
#       - ./ollama/ollama:/root/.ollama
#     container_name: ollama_for_rag
#     pull_policy: always
#     tty: true
#     restart: always
#     environment:
#       - OLLAMA_KEEP_ALIVE=24h
#       - OLLAMA_HOST=0.0.0.0

version: '3.9'
services:
  ollama:
    volumes:
      - ./ollama/ollama:/root/.ollama
    container_name: ollama_gpu
    pull_policy: always
    tty: true
    restart: unless-stopped
    image: ollama/ollama:latest
    ports:
      - 7869:11434
    environment:
      - OLLAMA_KEEP_ALIVE=24h
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]